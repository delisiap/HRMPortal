package com.hrm.rest.model;

import org.junit.Test;

public class EmployeeTest {
	@Test
	public void employeeLoginTestCases() {
		// EmployeeInfo employee = new EmployeeInfo();

		// employee.setAddress("1333 South Park Street");
		// employee.setAlternateEmailId("tuv@a.c");
		// employee.setDob("15081985");
		// employee.setEmailId("xyz@a.c");
		// employee.setEmployeeId("1234");
		// employee.setJoiningDate("12091978");
		// employee.setName("abc");
		// employee.setPhoneNumber(902978999);
		// employee.setRoleId("777");

		// assertEquals("1333 South Park Street", employee.getAddress());
		// assertEquals("tuv@a.c", employee.getAlternateEmailId());
		// assertEquals("15081985", employee.getDob());
		// assertEquals("xyz@a.c", employee.getEmailId());
		// assertEquals("1234", employee.getEmployeeId());
		// assertEquals("12091978", employee.getJoiningDate());
		// assertEquals("abc", employee.getName());
		// assertEquals(902978999, employee.getPhoneNumber());
		// // assertEquals("777", employee.getRoleId());

		// assertEquals(true, employee.getAddress() != null);
		// assertEquals(false, employee.getAddress() == null);

		// assertEquals(true, employee.getAlternateEmailId() != null);
		// assertEquals(false, employee.getAlternateEmailId() == null);

		// assertEquals(true, employee.getDob() != null);
		// assertEquals(false, employee.getDob() == null);

		// assertEquals(true, employee.getEmailId() != null);
		// assertEquals(false, employee.getEmailId() == null);

		// assertEquals(true, employee.getEmployeeId() != null);
		// assertEquals(false, employee.getEmployeeId() == null);

		// assertEquals(true, employee.getJoiningDate() != null);
		// assertEquals(false, employee.getJoiningDate() == null);

		// assertEquals(true, employee.getName() != null);
		// assertEquals(false, employee.getName() == null);

		// assertEquals(true, employee.getPhoneNumber() != 0);
		// assertEquals(false, employee.getRoleId() == 0);
	}
}
