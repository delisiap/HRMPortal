package com.hrm.rest.model;

import org.junit.Test;

public class RoleTest {
	@Test
	public void RoleTestCases() {
		// Role role = new Role();

		// role.setRole_id(777);
		// role.setRole_name("Manager");

		// assertEquals(777,role.getRole_id());
		// assertEquals("Manager",role.getRole_name());

		// assertEquals(false, role.getRole_id()==0);
		// assertEquals(true, role.getRole_id()!=0);

		// assertEquals(true, role.getRole_name()!=null);
		// assertEquals(true, role.getRole_name()==null);
	}
}
